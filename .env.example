VITE_APP_ENV=development
VITE_ENABLE_DEBUG_PANEL=false

# Optional local-only CrUX/PageSpeed monitoring key.
# Do not commit a real value. Put a real key in .env.local, exported shell env,
# or a GitHub Actions repository secret with one of these accepted names.
# CRUX_API_KEY=
# PAGESPEED_API_KEY=
# GOOGLE_API_KEY=
